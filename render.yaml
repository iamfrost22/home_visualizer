services:
  - type: web
    name: floorplan-to-3d-backend
    env: python
    buildCommand: "pip install -e .[serve]"
    startCommand: "uvicorn server.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
